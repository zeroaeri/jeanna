@import "tailwindcss";

/* 🎀 1. Define our Whimsy Color Palette */
:root {
  --bg-cream: #FDF8F5;
  --text-ink: #2A1C1A;
  --accent-pink: #F95292;
  --accent-tea: #B83955;
  --accent-yellow: #F4ECC2;
}

/* 🎀 2. Base Site Styles (Overrides Next.js defaults) */
body {
  background-color: var(--bg-cream);
  color: var(--text-ink);
  min-height: 100vh;
  position: relative;
  /* Use our Geist font as the default sans-serif */
  font-family: var(--font-sans), sans-serif; 
}

/* 🎀 3. The Highlight Magic (Hot pink when you select text!) */
::selection {
  background-color: var(--accent-pink);
  color: #ffffff;
}

/* 🎀 4. The Vintage Paper Noise Filter (Applied globally) */
body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  pointer-events: none; /* Lets you click through the texture */
  z-index: 9999;
  opacity: 0.04;
  mix-blend-mode: multiply;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
}